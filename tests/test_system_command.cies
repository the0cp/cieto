import "assert.cies";

$> exit 0
assert.eq(_exit_code, 0, "$> exit 0 exit code");

$> exit 7
assert.eq(_exit_code, 7, "$> exit 7 exit code");

$> echo cieto-system-test
assert.eq(_exit_code, 0, "$> echo exit code");

print "system command tests done";
